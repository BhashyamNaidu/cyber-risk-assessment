# Copy to .env and adjust as needed. Any value not set here falls back to
# the default in config.py.

BACKEND_URL=http://localhost:8000
DEVICE_ID_FILE=~/.cyberrisk_agent/device_id
REQUEST_TIMEOUT_SECONDS=10
LOG_LEVEL=INFO
LOG_FORMAT=json
